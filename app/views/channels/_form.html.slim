/ <div class="form-group msg-form">
/   <%= form_with(model: [@single_room ,@message], remote: true, class: "d-flex",
/      data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset" }) do |f| %>
/     <%= f.text_field :content, id: 'chat-text', class: "form-control msg-content", autocomplete: 'off' %>
/     <%= f.submit data: { disable_with: false }, class: "btn btn-primary" %>
/   <% end %>
/ </div>

.form-group.msg-form
  = form_with model: [channel, message], remote: true, class: 'd-flex', data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset"} do |f|
    = f.text_field :body, id: 'chat-text', class: 'form-control shadow-none msg-control me-2', autocomplete: 'off', placeholder: t('pages.conversation.placeholder.new_message')
    = f.submit data: { diable_with: false }, class: 'btn btn-primary'